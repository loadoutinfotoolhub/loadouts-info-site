# Deploy

loadouts.info — coming soon.

Cloudflare Pages, Build `pnpm build`, Output `dist`, Production branch `main`.
Custom domain `loadouts.info` ist bereits verbunden. Auto-Deploy bei Push auf `main`.
